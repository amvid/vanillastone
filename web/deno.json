{
  "nodeModulesDir": "auto",
  "tasks": {
    "dev": "deno run -A npm:vite",
    "build": "deno run -A npm:typescript/tsc && deno run -A npm:vite build",
    "preview": "deno run -A npm:vite preview"
  }
}
